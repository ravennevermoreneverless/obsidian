views:
  - type: cards
    name: films (general)
    filters:
      and:
        - "!dateWatched.isEmpty()"
    sort:
      - property: file.ctime
        direction: DESC
    image: note.image
    imageAspectRatio: 1.5
