views:
  - type: cards
    name: films (general)
    filters:
      and:
        - "!dateWatched.isEmpty()"
    order:
      - file.name
      - title
      - artist
    sort:
      - property: file.ctime
        direction: DESC
    image: note.image
    imageAspectRatio: 1.5
